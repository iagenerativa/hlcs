grpcio==1.60.0
grpcio-tools==1.60.0
grpcio-reflection==1.60.0
grpcio-health-checking==1.60.0
protobuf==4.25.1

# REST Gateway
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# HTTP Client (for SARAi MCP if REST fallback)
httpx==0.26.0
aiohttp==3.9.1

# Utilities
python-dotenv==1.0.0
pyyaml==6.0.1

# RAG & Memory (ChromaDB + LangChain)
chromadb>=0.4.22  # Persistent vector storage
sentence-transformers>=2.2.2  # all-MiniLM-L6-v2 embeddings (~50MB)
langchain>=0.1.0  # Orchestration (lightweight)
langchain-community>=0.0.20  # Community integrations
numpy>=1.24.0  # For embeddings

# Logging & Monitoring
structlog==24.1.0
prometheus-client==0.19.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-httpx==0.27.0

# Development
black==23.12.1
ruff==0.1.9
mypy==1.8.0
